let members :string[]=[
    'amir','mohammad','azad','mussa','feroz']
    if(members.length === 0){
        console.log("we need to find some users! ")
    }
    else{
        members=[]
        console.log("all users are removed"  + members.length);
        
    }